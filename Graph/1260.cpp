#include <stdio.h>
#include <queue>
#include <algorithm>
#include <cstring>
#include <vector>
using namespace std;

vector<int> map[1001];
bool check[1001];
unsigned int N, M ,V;

void dfs(int node);


int main(){
    scanf("%d %d %d", &N,v&M, &V);
    for(int i =0; i<M; i++){
        int u, v; 
        scanf("%d %d",&u, &v );


    }
    

    return 0;
}